import React from 'react';
import { GlassChartCard } from '../glass/GlassChartCard';

export const ClayChartCard = (props) => {
  return <GlassChartCard {...props} />;
};

export default ClayChartCard;
